name: Apply comprehensive issue guidance once

on:
  pull_request:
    branches:
      - main
    types:
      - opened
      - reopened
      - synchronize
      - edited

permissions:
  contents: write

concurrency:
  group: apply-comprehensive-issue-guidance-once
  cancel-in-progress: false

jobs:
  apply:
    if: >-
      github.event.pull_request.head.repo.full_name == github.repository &&
      github.head_ref == 'ops/comprehensive-issue-upgrade' &&
      github.event.pull_request.title == 'chore(roadmap): patch canonical milestone timestamps'
    runs-on: ubuntu-latest
    timeout-minutes: 15
    env:
      GH_TOKEN: ${{ github.token }}
      GITHUB_REPOSITORY: ${{ github.repository }}
      PATCH_BRANCH: ops/comprehensive-issue-upgrade
      PR_NUMBER: ${{ github.event.pull_request.number }}

    steps:
      - name: Checkout exact reviewed PR head
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          ref: ${{ github.event.pull_request.head.sha }}
          persist-credentials: false

      - name: Verify one-shot patch scope
        shell: bash
        run: |
          set -Eeuo pipefail
          gh api --paginate \
            "repos/$GITHUB_REPOSITORY/pulls/$PR_NUMBER/files?per_page=100" |
            jq -s 'add // [] | map(.filename)' >changed-files.json

          jq -e '
            all(.[ ];
              test("^(roadmap/|scripts/bootstrap-roadmap[.]sh$|tests/roadmap/|docs/superpowers/|scripts/apply-comprehensive-issue-upgrade-once[.]sh$|scripts/patch-milestone-canonical-time-once[.]sh$|[.]github/workflows/apply-comprehensive-issue-upgrade-once[.]yml$|[.]github/workflows/ci[.]yml$|[.]github/workflows/bootstrap-roadmap[.]yml$)")
            )
          ' changed-files.json >/dev/null

      - name: Patch canonical milestone timestamps and run regression tests
        shell: bash
        run: bash scripts/patch-milestone-canonical-time-once.sh
